version: 2

project_name: uictl

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - main: .
    binary: uictl
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/kfriede/uictl/cmd.Version={{.Version}}
      - -X github.com/kfriede/uictl/cmd.Commit={{.ShortCommit}}
      - -X github.com/kfriede/uictl/cmd.BuildDate={{.Date}}

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: kfriede
    name: uictl

brews:
  - repository:
      owner: kfriede
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/kfriede/uictl"
    description: "CLI for managing UniFi network devices and controllers — built for humans and LLM agents"
    license: "MIT"
    install: |
      bin.install "uictl"
      generate_completions_from_executable(bin/"uictl", "completion")
    test: |
      system "#{bin}/uictl", "version"
