version: "2"

linters:
  default: standard
  disable:
    - exhaustruct
    - wrapcheck
    - varnamelen
    - funlen
    - cyclop
    - gocognit
    - nestif
    - godox

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
